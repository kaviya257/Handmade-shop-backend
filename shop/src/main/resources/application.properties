server.port=${PORT:8080}

# Use variables so Render can fill them in automatically
spring.datasource.url=jdbc:postgresql://dpg-d7j3p79f9bms738grcm0-a.oregon-postgres.render.com/shopdb_9n3y
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Hibernate 6+ automatically detects the dialect, but this doesn't hurt:
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
